# Assume you have a methog isSubstring, that checks if one work is a substring of another. Given two strings s1 and s2, check if s2 is a rotation of s1 using only one call to isSubstring
#
# Example: "waterbottle" is a rotation of "erbottlewat"